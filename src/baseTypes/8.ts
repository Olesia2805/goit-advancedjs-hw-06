/*
  Create a 'Gender' type using the union type, 
  which can contain the values 'male', 'female'. Create a myGender variable of this type.
*/
type Gender = 'male' | 'female';
const myGender: Gender = 'female';

export {};
